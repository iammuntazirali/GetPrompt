#: Add unit tests for server endpoints (vitest + supertest)

Description
-----------
Write tests for GET, POST, and PATCH endpoints using Vitest and Supertest.

Acceptance criteria
-------------------
- Tests are added under `apps/server/test/` and run with `npm test:unit`.
- CI runs these tests and fails the job if any test fails.

Labels: `tests`, `backend`

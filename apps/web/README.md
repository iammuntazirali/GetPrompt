# web-vite

A simple Vite + React TypeScript frontend scaffold for PromptHub. This directory was generated automatically by the assistant as a minimal project structure. Install dependencies and run `npm run dev` to start.

Quick start:

```bash
cd apps/web-vite
npm install
npm run dev
```
